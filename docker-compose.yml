services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DJANGO_DEBUG=True
      # Match Dockerfile CMD (gunicorn binds to PORT; default 8080 would not match 8000:8000)
      - PORT=8000
